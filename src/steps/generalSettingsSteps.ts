// import { Given, When, Then } from "@cucumber/cucumber";

// Given('the user is logged in', async function () {

// });

// When('the user navigates to the General Settings page', async function () {
//   await pageFixture.page.getByRole('button').nth(2).click();
// });

// When('the user updates the pre-filled data with:', async function (dataTable) {
//     console.log(dataTable)
//   const data = dataTable.rowsHash();

//   // Update the fields with the new values using fake locators
//   await pageFixture.page.locator("input[name='clubName']").fill(data['Club Name'])
//   await pageFixture.page.locator("input[name='primaryEmail']").fill(data['Primary Email']);
//   // ... fill other fields similarly ...
// });

// When('the user clicks on "save"', async function () {
//   // Implement clicking on the "save" button
//   await pageFixture.page.locator("button[data-testid='save-button']").click();
// });

// Then('the changes should be saved successfully', async function () {
//   // Implement verification that changes were saved successfully
//   // For example, wait for a success message or check if the page reflects the changes
// });
