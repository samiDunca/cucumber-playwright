export type startAndEndDate = {
  hourFromFirstArray: number;
  hourFromSecondArray: number;
};

export type scheduleData = {
  randomTwoDigitNumber: number;
  scheduleName: string;
  hourFromFirstArray: number;
  hourFromSecondArray: number;
  overrideName: string;
  overrideAmount: number;
};

export type rateOverrideData = {
  overrideName: string;
  amount: number;
};
